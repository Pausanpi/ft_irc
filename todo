cuando entras por nc, manda un mensaje que server is full pero sí que deja entrar

cuando se desconecta, no avisa en el servidor de que se ha ido alguien.

si pones /join # -> no debería crear un canal, debería decir que parametros insuficientes

kick debe poner OP al usuario más antiguo que entró

por defecto sale mode +nt, debería salir solo mode +. cuando crea el canal solo puede cambiar el topic el OP.

cuando hago +i debería aparecer +nti, pero no se añade.

en nc, cuando introducimos pass, tiene que hacer pass nick y user. queremos un mensaje de aceptado o rechazado.

en privmsg si ponemos : pilla todo, pero si no ponemos : solo pasa el primer argumento.

